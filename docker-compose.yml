version: '3'

services:
  manager:
    container_name: manager
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /var/run/docker.sock:/var/run/docker-host.sock
    environment:
      - DEBUG=1
      - TRACE=1
      - PRIVATE_KEY=DEMO
